<%- include('../partials/header', { title: 'Crear cuenta' }) %>

<div class="container container-narrow">
  <div class="card">
    <div class="card-header text-center">
      <h1 class="card-title">Crear cuenta</h1>
      <p class="text-muted">Registrate para acceder a la mesa de ayuda.</p>
    </div>
    <div class="card-body">
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-error">
          <span><%= error %></span>
        </div>
      <% } %>
      <form method="post" action="/register">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <div class="form-group">
          <label class="form-label" for="username">Usuario</label>
          <input class="form-control" id="username" name="username" required autofocus />
        </div>
        <div class="form-group">
          <label class="form-label" for="email">Email</label>
          <input class="form-control" id="email" name="email" type="email" required />
        </div>
        <div class="form-group">
          <label class="form-label" for="password">Contraseña</label>
          <input class="form-control" id="password" name="password" type="password" required />
        </div>
        <button type="submit" class="btn btn-primary btn-block btn-lg">Registrarme</button>
      </form>
    </div>
    <div class="card-footer">
      <a href="/login" class="btn btn-outline btn-block">Ya tengo una cuenta</a>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
