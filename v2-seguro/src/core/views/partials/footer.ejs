    </main>
    <!--
      CSP note (src/config/helmet.js restricts scriptSrc to 'self'): v1's
      footer loads Lucide icons from the unpkg.com CDN. That script tag is
      intentionally NOT ported here — it would be blocked by CSP, and icons
      are out of scope for the auth module. Plain text/buttons only.
    -->
  </body>
</html>
